# Environment Variables Template for Deployment
# Copy this file and replace values with your actual configuration

# ===========================================
# RENDER.COM BACKEND ENVIRONMENT VARIABLES
# ===========================================
# Add these in Render dashboard under "Environment"

# Application Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:10000

# Database Connection (Replace with your Azure SQL or PostgreSQL connection string)
ConnectionStrings__DefaultConnection=Server=YOUR_SERVER.database.windows.net,1433;Database=MoneyManagerDb;User Id=YOUR_USERNAME;Password=YOUR_PASSWORD;TrustServerCertificate=True;MultipleActiveResultSets=true

# JWT Settings
JwtSettings__SecretKey=YourSuperSecretKeyForJWTTokenGeneration12345!
JwtSettings__Issuer=MoneyManagerAPI
JwtSettings__Audience=MoneyManagerClient
JwtSettings__ExpirationInMinutes=1440

# ===========================================
# AZURE SQL DATABASE CONNECTION STRING EXAMPLES
# ===========================================

# Standard Azure SQL connection string format:
# Server=tcp:YOUR_SERVER.database.windows.net,1433;Initial Catalog=MoneyManagerDb;Persist Security Info=False;User ID=YOUR_USERNAME;Password=YOUR_PASSWORD;MultipleActiveResultSets=True;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# ===========================================
# POSTGRESQL CONNECTION STRING (if using ElephantSQL)
# ===========================================

# PostgreSQL format (requires code changes to use Npgsql):
# Host=YOUR_HOST.db.elephantsql.com;Database=YOUR_DB;Username=YOUR_USER;Password=YOUR_PASSWORD;Port=5432

# ===========================================
# NETLIFY ENVIRONMENT VARIABLES (if needed)
# ===========================================

# Usually not needed for static frontend
# But you can add if using Netlify Functions:
# API_URL=https://your-backend.onrender.com

# ===========================================
# LOCAL DEVELOPMENT (.env file)
# ===========================================

# For local development, you can create a .env file
# This is NOT used in production - only for local testing
